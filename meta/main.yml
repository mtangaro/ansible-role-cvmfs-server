galaxy_info:
  author: Marco Antonio Tangaro
  description: Ansible role to setup a CernVM-FS stratum zero repository
  company: IBIOM-CNR, ELIXIR-IIB
  license: Apache
  min_ansible_version: 2.0
  min_ansible_version: 2.1.2.0
  platforms:
  - name: Ubuntu
    versions:
    - xenial
  #- name: EL
  #  versions:
  #  - 7
  galaxy_tags:
    - cvmfs
    - filesystem
dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
