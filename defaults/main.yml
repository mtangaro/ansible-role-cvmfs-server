---
# defaults file for ansible-role-cvmfs-server

repository_name: 'elixir-italy.galaxy.refdata'

stratum_0: '{{ ansible_default_ipv4.address }}'

repository_url: 'http://{{ stratum_0 }}/cvmfs/{{ repository_name }}'

