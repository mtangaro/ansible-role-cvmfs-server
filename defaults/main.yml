---
# defaults file for ansible-role-cvmfs-server

repository_name: 'elixir-italy.galaxy.refdata'

stratum_zero: '{{ ansible_default_ipv4.address }}'

repository_url: 'http://{{ stratum_zero }}/cvmfs/{{ repository_name }}'
